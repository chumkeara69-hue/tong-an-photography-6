import {NextResponse} from "next/server"; import {getAdminId} from "@/lib/auth"; import {prisma} from "@/lib/db";
export async function POST(req:Request,{params}:{params:Promise<{id:string}>}){if(!(await getAdminId()))return NextResponse.json({error:"Unauthorized"},{status:401});const {id}=await params;const {action}=await req.json();if(!["approve","reject"].includes(action))return NextResponse.json({error:"Invalid action"},{status:400});const status=action==="approve"?"APPROVED":"REJECTED";await prisma.$transaction([prisma.payment.update({where:{orderId:id},data:{status,reviewedAt:new Date()}}),prisma.order.update({where:{id},data:{status:action==="approve"?"PAID":"REJECTED"}})]);return NextResponse.json({ok:true})}
