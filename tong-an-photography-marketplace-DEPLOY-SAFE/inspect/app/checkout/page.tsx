"use client"; import {useEffect,useState} from "react"; import {useRouter} from "next/navigation";
export default function Checkout(){const [items,setItems]=useState<any[]>([]);const [form,setForm]=useState({name:"",email:"",phone:""});const [loading,setLoading]=useState(false);const router=useRouter();useEffect(()=>{try{setItems(JSON.parse(localStorage.getItem("tong_cart")||"[]"))}catch{setItems([])}},[]);const total=items.reduce((s,x)=>s+x.price,0);async function submit(e:any){e.preventDefault();if(!items.length)return;setLoading(true);const r=await fetch("/api/orders",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({...form,items:items.map(x=>x.id)})});const d=await r.json();setLoading(false);if(r.ok){localStorage.removeItem("tong_cart");router.push(`/payment-status/${d.orderId}`)}else alert(d.error||"Unable to create order")}if(!items.length)return <main><div className="container checkout"><div className="summary"><div className="eyebrow">Your cart</div><h2 className="serif">Nothing to checkout</h2><p className="muted">Add a photograph to your cart first.</p><a className="btn primary" href="/gallery">Explore gallery</a></div></div></main>; return <main><div className="container checkout"><div className="sectionhead"><div><div className="eyebrow">Secure order</div><h2>Checkout</h2></div></div><div className="checkoutgrid"><form onSubmit={submit} className="summary"><div className="field"><label>FULL NAME</label><input required value={form.name} onChange={e=>setForm({...form,name:e.target.value})}/></div><div className="field"><label>EMAIL</label><input required type="email" value={form.email} onChange={e=>setForm({...form,email:e.target.value})}/></div><div className="field"><label>PHONE</label><input value={form.phone} onChange={e=>setForm({...form,phone:e.target.value})}/></div><button className="btn primary" disabled={loading}>{loading?"Creating order…":"Continue to payment"}</button></form><aside className="summary"><div className="muted">Items</div>{items.map(x=><div className="row" key={x.id} style={{margin:"10px 0"}}><span>{x.title}</span><span>${x.price.toFixed(2)}</span></div>)}<hr style={{borderColor:"var(--line)"}}/><div className="row"><strong>Total</strong><span className="total">${total.toFixed(2)}</span></div></aside></div></div></main>}
