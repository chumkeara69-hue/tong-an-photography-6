"use client"; import {useState} from "react"; import {useRouter} from "next/navigation";
export default function AddToCart({photo}:{photo:any}){const [added,setAdded]=useState(false);const router=useRouter();function add(){const cart=JSON.parse(localStorage.getItem("tong_cart")||"[]");if(!cart.some((x:any)=>x.id===photo.id))cart.push(photo);localStorage.setItem("tong_cart",JSON.stringify(cart));setAdded(true);router.refresh()}return <div className="actions"><button className="btn primary" onClick={add}>{added?"Added to cart ✓":"Add to cart"}</button>{added&&<button className="btn" onClick={()=>router.push("/cart")}>View cart</button>}</div>}
