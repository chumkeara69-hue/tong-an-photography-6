import {prisma} from "@/lib/db"; import OrderActions from "./OrderActions";
export default async function Orders(){const orders=await prisma.order.findMany({include:{payment:true,items:{include:{photo:true}}},orderBy:{createdAt:"desc"}});return <><div className="eyebrow">Payments</div><h1 className="serif" style={{fontSize:48}}>Orders</h1><div className="summary" style={{overflowX:"auto"}}><table className="table"><thead><tr><th>Order</th><th>Customer</th><th>Total</th><th>Receipt</th><th>Status</th><th></th></tr></thead><tbody>{orders.map(o=><tr key={o.id}><td>#{o.id.slice(-8)}</td><td>{o.customerName}<br/><span className="muted">{o.customerEmail}</span></td><td>${Number(o.total).toFixed(2)}</td><td>{o.payment?.receiptKey?"Uploaded":"—"}</td><td>{o.payment?.status||"—"}</td><td><OrderActions id={o.id}/></td></tr>)}</tbody></table></div></>}
