"use client"; import {useRouter} from "next/navigation"; import {useState} from "react";
export default function OrderActions({id}:{id:string}){const [busy,setBusy]=useState(false);const router=useRouter();async function act(action:string){setBusy(true);const r=await fetch(`/api/admin/orders/${id}`,{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({action})});setBusy(false);if(r.ok)router.refresh();else alert((await r.json()).error||"Failed")}return <div className="actions">{<button className="btn" disabled={busy} onClick={()=>act("approve")}>Approve</button>}<button className="btn" disabled={busy} onClick={()=>act("reject")}>Reject</button></div>}
