"use client"; import {useState} from "react"; import {useRouter} from "next/navigation";
export default function Login(){const [email,setEmail]=useState("");const [password,setPassword]=useState("");const [error,setError]=useState("");const router=useRouter();async function go(e:any){e.preventDefault();setError("");const r=await fetch("/api/admin/login",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({email,password})});if(r.ok)router.push("/admin");else setError((await r.json()).error||"Login failed")}return <main><div className="container section" style={{maxWidth:520}}><div className="eyebrow">Private area</div><h1 className="serif" style={{fontSize:48}}>Admin Login</h1><form className="summary" onSubmit={go}><div className="field"><label>EMAIL</label><input type="email" required value={email} onChange={e=>setEmail(e.target.value)}/></div><div className="field"><label>PASSWORD</label><input type="password" required value={password} onChange={e=>setPassword(e.target.value)}/></div>{error&&<div className="notice">{error}</div>}<button className="btn primary" style={{marginTop:14}}>Sign in</button></form></div></main>}
