import {prisma} from "@/lib/db"; import PhotoUpload from "./PhotoUpload";
export default async function Photos(){const photos=await prisma.photo.findMany({orderBy:{createdAt:"desc"}});return <><div className="eyebrow">Catalog</div><h1 className="serif" style={{fontSize:48}}>Manage Photos</h1><PhotoUpload/><div className="summary" style={{marginTop:25,overflowX:"auto"}}><table className="table"><thead><tr><th>Title</th><th>Category</th><th>Price</th><th>Published</th></tr></thead><tbody>{photos.map(p=><tr key={p.id}><td>{p.title}</td><td>{p.category}</td><td>${Number(p.price).toFixed(2)}</td><td>{p.isPublished?"Yes":"No"}</td></tr>)}</tbody></table></div></>}
