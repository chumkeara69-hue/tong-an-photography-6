 "use client";
import Link from "next/link"; import Image from "next/image"; import {useState} from "react";
export default function GalleryClient({photos}:{photos:any[]}){const cats=["All",...Array.from(new Set(photos.map(p=>p.category)))];const [cat,setCat]=useState("All");const shown=cat==="All"?photos:photos.filter(p=>p.category===cat);return <main><div className="container gallerytop"><div className="eyebrow">The collection</div><h1>Gallery</h1><p className="muted">High-quality digital originals, delivered after payment approval.</p><div className="filters">{cats.map(c=><button key={c} className={`filter ${cat===c?"active":""}`} onClick={()=>setCat(c)}>{c}</button>)}</div><div className="grid">{shown.map(p=><Link className="card" key={p.id} href={`/photo/${p.slug}`}><div className="thumb"><Image src={p.previewKey} alt={p.title} width={700} height={520}/></div><div className="cardbody"><div className="row"><h3 className="cardtitle">{p.title}</h3><span className="price">${p.price.toFixed(2)}</span></div><p className="muted">{p.description}</p></div></Link>)}</div></div></main>}
